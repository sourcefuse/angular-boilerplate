export interface Subscriber {
  firstName: string;
  lastName: string;
  contact: string;
  companyName: string;
  email: string;
  address?: string;
  country: string;
  zip?: string;
  subdomain: string;
  regToken: string;
}
