export interface Subscriber {
  firstName: string;
  lastName: string;
  companyName: string;
  email: string;
  address?: string;
  country: string;
  zip?: string;
  key: string;
  regToken: string;
}
